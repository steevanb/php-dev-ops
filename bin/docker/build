#!/usr/bin/env bash

set -eu

readonly PROJECT_PATH="$(realpath "$(dirname "$(realpath "${BASH_SOURCE[0]}")")/../..")"

source "${PROJECT_PATH}"/config/docker/docker-image-name.inc.bash

docker build --tag "${DOCKER_IMAGE_NAME}" "${PROJECT_PATH}"/docker
